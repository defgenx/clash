{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window permissions — core:default includes the event listen/emit permissions the frontend needs for pty-output streaming.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
